{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}

<div class="maincontainer">
<div class="page-{{page}}">
 <h2>{{page|capitalize}}</h2>


    {{form_start(contact, {'attr': {'class': 'd-grid  justify-content-center'}} ) }}
          {{form_rest(contact)}}  
          {{form_end(contact)}}   
        


</div>
</div>
{% endblock %}
